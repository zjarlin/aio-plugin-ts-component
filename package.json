{
  "name": "aio-plugin-ts-component",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "build:js": "tsc -p tsconfig.json",
    "build:component": "jco componentize build/plugin/component.js --wit wit --world-name page --disable all -o dist/plugin.wasm",
    "build": "pnpm run build:js && pnpm run build:component",
    "test": "pnpm run build:js && node --test test/**/*.test.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@bytecodealliance/componentize-js": "0.22.0",
    "@bytecodealliance/jco": "1.32.1",
    "typescript": "7.0.2"
  }
}
